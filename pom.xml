<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.innovarhealthcare</groupId>
    <artifactId>bridge-link-launcher</artifactId>
    <version>1.6.0</version>

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <commons-lang3.version>3.12.0</commons-lang3.version>
        <jackson-databind.version>2.13.3</jackson-databind.version>
        <javafx.version>17.0.10</javafx.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson-databind.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.innovarhealthcare.launcher.Main</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Windows release build: wraps the shaded jar in a native .exe launcher
            (launch4j) configured to use the JRE embedded in the install dir
            (jre/, provisioned by build/windows/setup-jre.ps1 before packaging).

            Activate with:  mvn -Pwindows-release package
        -->
        <profile>
            <id>windows-release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.akathist.maven.plugins.launch4j</groupId>
                        <artifactId>launch4j-maven-plugin</artifactId>
                        <version>2.7.0</version>
                        <executions>
                            <execution>
                                <id>launch4j</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>launch4j</goal>
                                </goals>
                                <configuration>
                                    <headerType>gui</headerType>
                                    <jar>${project.build.directory}/${project.build.finalName}.jar</jar>
                                    <outfile>${project.build.directory}/BridgeLinkLauncher.exe</outfile>
                                    <errTitle>BridgeLink Launcher</errTitle>
                                    <downloadUrl>https://adoptium.net/temurin/releases/</downloadUrl>
                                    <stayAlive>false</stayAlive>
                                    <!-- Run from the install dir so relative paths
                                         (jre/, lib/, data/, cache/) resolve correctly. -->
                                    <chdir>.</chdir>
                                    <icon>${project.basedir}/build/windows/BridgeLinkLauncher.ico</icon>
                                    <!-- Prefer the embedded JRE shipped by the installer. -->
                                    <jre>
                                        <path>jre</path>
                                        <minVersion>17</minVersion>
                                        <requiresJdk>false</requiresJdk>
                                        <requires64Bit>true</requires64Bit>
                                    </jre>
                                    <!-- Heap for the launcher UI itself (Mirth gets its
                                         own JVM, configured per connection). -->
                                    <initialHeapSize>134217728</initialHeapSize>
                                    <maxHeapSize>536870912</maxHeapSize>
                                    <versionInfo>
                                        <fileVersion>${project.version}.0</fileVersion>
                                        <txtFileVersion>${project.version}</txtFileVersion>
                                        <fileDescription>BridgeLink Administrator Launcher</fileDescription>
                                        <copyright>Mozilla Public License 2.0</copyright>
                                        <productVersion>${project.version}.0</productVersion>
                                        <txtProductVersion>${project.version}</txtProductVersion>
                                        <productName>BridgeLink Administrator Launcher</productName>
                                        <companyName>Innovar Healthcare</companyName>
                                        <internalName>BridgeLinkLauncher</internalName>
                                        <originalFilename>BridgeLinkLauncher.exe</originalFilename>
                                    </versionInfo>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
